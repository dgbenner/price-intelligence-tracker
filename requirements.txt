# Core dependencies for the price tracker prototype

# For future scraping implementation (currently using placeholders)
# requests==2.31.0
# beautifulsoup4==4.12.2
# selenium==4.15.0

# No additional dependencies needed for the current prototype
# Everything uses Python standard library (sqlite3, datetime, etc.)
